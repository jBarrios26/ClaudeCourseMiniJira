meta {
  name: Create User (admin)
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/users
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Jane Doe",
    "email": "jane@example.com",
    "password": "secret123",
    "role": "user"
  }
}
